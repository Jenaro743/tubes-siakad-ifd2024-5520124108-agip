<x-app-layout><x-slot name="header"><h1 class="h3 mb-0">Detail Jadwal</h1><a class="btn btn-outline-secondary" href="{{ route('admin.jadwal.index') }}">Kembali</a></x-slot><div class="card border-0 shadow-sm"><div class="card-body"><h2 class="h5">{{ $jadwal->mataKuliah->nama_mk }}</h2><p>Dosen: {{ $jadwal->dosen->nama_dosen }}</p><p>{{ $jadwal->hari }}, {{ substr($jadwal->jam_mulai,0,5) }} - {{ substr($jadwal->jam_selesai,0,5) }} | Kelas {{ $jadwal->kelas }} | {{ $jadwal->ruangan }}</p><h3 class="h6">Mahasiswa Mengambil</h3><ul class="list-group">@forelse($jadwal->krs as $item)<li class="list-group-item">{{ $item->mahasiswa->npm }} - {{ $item->mahasiswa->nama_mahasiswa }}</li>@empty<li class="list-group-item text-muted">Belum ada peserta.</li>@endforelse</ul></div></div></x-app-layout>
